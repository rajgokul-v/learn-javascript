const a = 100
const b = 15
let result

/* Check the results by uncommenting below statements one after another */
/* Simple math operations */
result = a + b
// result = a - b
// result = a * b
// result = a / b
// result = a % b

/* Check the results by uncommenting below statements one after another */
/* Math object */
// result = Math.PI // returns the value of PI
// result = Math.round(56.324) // rounds to the nearest integer
// result = Math.ceil(89.652) // returns to the nearest higher integer , 90
// result = Math.floor(89.652) // returns to the nearest lower integer, 89
// result = Math.sqrt(64) // returns square root
// result = Math.pow(5, 3) // returns power of first value to the second value (5^3, 5 cube)
// result = Math.abs(-8) // returns absolute value (positive value)
// result = Math.min(2, 6, 10, 5, 8, -3, -1) // returns the minimum value in the list
// result = Math.min(2, 6, 10, 5, 8, -3, -1) // returns the maximum value in the list
// result = Math.random() // returns a value between 0 to 1. See below usecase
// result = Math.floor(Math.random() * 10 + 1) // gives a random number from 1 to 10

console.log(result)
